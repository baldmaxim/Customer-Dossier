# Пользовательский прогон этапа XX
**Это форма, а не выполненный прогон.**

## До запуска
Версия/diff, machine/Node/npm, выбранная тестовая цель, её marker/guard, состояние flags и .env (только редактированные сведения). Рабочие credentials не прикладывать. Агент не должен проверять соединение вместо пользователя.

| Шаг | Точная команда и cwd | Изменяет данные? | Цель/guard | Ожидается | Фактически | Exit/log |
|---|---|---|---|---|---|---|
| Заполнить | Указать после реализации | Да/нет | Выделенная test | Отдельно | NOT_RUN | Нет |

## Последовательность
Сначала проверка разрешённой цели, затем собственно действие. Для backup: остановить писателей → baseline → dump → отдельная test restore → compare → приложение. Benchmark после сравнения, потому что создаёт snapshots.

## Что передать обратно
Редактированный log, summary JSON, commit/diff, итог всех файлов тестов, ID failed cases. Не пересылать passwords/tokens/dump реальных материалов по умолчанию. Для model evaluation — exact config/corpus/scorer hashes и outcome, без правдоподобных чисел «по ощущениям».

## Итог
PASS/FAIL/NOT_RUN по каждой области. Не начатое из-за отсутствия доступа остаётся NOT_RUN. Следующее согласованное действие.
