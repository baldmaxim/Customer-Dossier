# RELEASE_READINESS — готовность локальной установки

Дата: 2026-09-16. Этап 09 (сквозная приёмка). Область — **локальная установка у одного оператора**.
Это не сертификат промышленной готовности и не заявление о юридической достоверности или полноте сведений
о компаниях. Портал показывает, что написано в собранных публикациях, и с какими оговорками.

## Итоговые статусы

| Статус | Значение | Основание |
|---|---|---|
| **LOCAL_FIXTURE_READY** | **да** (core-gates, прогон пользователя 2026-09-16; печать в PDF не проверялась) | сквозной сценарий на синтетическом корпусе: `release/release.int.test.ts`, seed `npm run seed:test-release`, раздел N в `TESTING_LOCAL.md` |
| **LOCAL_MODEL_VALIDATED** | **нет** | замер qwen3-8b на extract@3 (этап 06): безопасность 24/25, полнота 3/9. Модель пригодна для проверки конвейера, но не для массового переразбора рабочей базы |
| **LIVE_SOURCE_VALIDATED** | **нет, список пуст** | ни один живой источник не имеет подтверждённого допуска; живые пробы, сбор и бот не запускались ни разу |
| PRODUCTION_READY | **не заявляется** | вне области этапа: одна машина, один оператор, loopback |

## Что проверено и чем

| Область | Как проверено | Статус |
|---|---|---|
| Воспроизводимая установка | `npm ci` по lock-файлам, backend и frontend | PASS (прогон пользователя 08B; зависимости на 09 не менялись) |
| Сборка и типы | `tsc --noEmit`, `npm run build` (оба пакета) | PASS |
| Unit | `npm test` — 29 файлов / 421 тест, без БД и сети | PASS |
| PostgreSQL-интеграция 01–09 | `npm run test:integration` — 18 файлов / 196 тестов | PASS (прогон пользователя 09) |
| Установка с нуля и апгрейд синтетической legacy-базы | `release.int.test.ts`, TC-074 | PASS |
| Сквозной путь источник → досье → снимок → перезапуск | `release.int.test.ts` + сценарий N4 через API, TC-076 | PASS |
| Отказы: БД, модель, схема ответа, отзыв допуска, доступ, опасный фрагмент | `release.int.test.ts` + N5, TC-077 | PASS |
| Контрольные числа и восстановление копии | pg_dump → pg_restore в отдельную цель → `release:check --compare`, TC-075 | PASS (совпадение; искусственное расхождение обнаружено) |
| Производительность | `npm run release:bench`, TC-078 | PASS: baseline в `stages/09_REPORT.md`; время поиска не измерено |
| Печать в PDF, 390 px | вручную | NOT_RUN (сценарий N4 пройден через API) |
| Живые источники, живой бот, реальная модель под нагрузкой | — | NOT_RUN (нет допуска и ключей) |
| Миграция реальных данных пользователя | — | NOT_RUN: рабочая база не идентифицирована и не подключалась; проверялась **синтетическая** legacy-база |

## Границы заявления

- Проверено на синтетическом корпусе, созданном для этой проверки. Реальные тексты, реальные компании
  и реальная нагрузка не участвовали.
- «Миграции воспроизводимы» относится к пустой базе и к синтетической базе, накаченной до 009.
  Апгрейд рабочей копии пользователя не выполнялся и не заявляется.
- Сохранность данных ограничена сохранностью диска и регулярностью резервных копий; автоматических копий нет.
- Локальная модель не валидирована по полноте: пропуски фактов вероятны, и это видно в замере этапа 06.
- Итоговой оценки надёжности компаний нет; отсутствие сигналов не означает отсутствие проблем.

## Условия, при которых статус меняется

- **LOCAL_FIXTURE_READY → да**: пользователь прогнал разделы A и N (включая `release:check` до и после
  восстановления копии) без падений.
- **LOCAL_MODEL_VALIDATED → да**: повторный `npm run benchmark:model` на текущей схеме даёт приемлемую полноту
  при сохранении безопасности, и решение зафиксировано отдельным отчётом.
- **LIVE_SOURCE_VALIDATED → перечень**: для каждого источника — подтверждённый допуск с основанием, успешная
  проба и хотя бы один полный проход сбора без `parser_degraded`.

## Незакрытое (severity — в `BACKLOG.md`)

Блокирующих дефектов (P1) на момент составления нет. Открытые P2/P3 — качество полноты локальной модели,
отсутствие UI для очереди неоднозначностей, перенос решений аналитика при слиянии, отсутствие
автоматической очистки и копий, отсутствие frontend-тестов.
