# Пользовательский прогон этапа 15A
**Это форма, а не выполненный прогон.** PowerShell, корень `TG_Info`. Тестовая цель — `127.0.0.1:55433/tg_info_test` с маркером.
Рабочая база и реальные слияния не используются. Все компании и реквизиты — синтетические.

| Шаг | Команда (cwd) | Изменяет данные? | Цель/guard | Ожидается | Фактически | Exit/log |
|---|---|---|---|---|---|---|
| 1 | `cd backend; npx vitest run src/resolve src/api src/dossier --maxWorkers=2` | нет | мёртвый адрес БД | все passed | NOT_RUN | — |
| 2 | `Remove-Item Env:DATABASE_URL -ErrorAction SilentlyContinue; $env:TEST_DATABASE_URL='postgresql://tg_test:tg_test@127.0.0.1:55433/tg_info_test'; npx vitest run -c vitest.integration.config.ts src/resolve/identity.int.test.ts` | да: пересоздаёт схему, миграции 001–023 | общий preflight (имя, loopback, роль, маркер) | все passed, включая describe «этап 15A» (6 тестов) | NOT_RUN | — |
| 3 | `npm run test:integration` | да: то же | то же | все файлы passed, 0 skipped (в т.ч. `snapshot.int` — старый снимок при слиянии) | NOT_RUN | — |
| 4 (браузер) | API + UI на `tg_info_test` после seed (`evidence/09/USER_RUN_CLOSURE.md` раздел C), `MERGE_APPLY_ENABLED=true` только в оболочке тестового API | да: решения, слияние в тестовой базе | тестовая цель | см. ниже | NOT_RUN | — |
| 5 | Раздел E `evidence/09/USER_RUN_CLOSURE.md` на данных шага 4 | да: restore-цель | маркер restore-цели | `MATCH`, таблица `ambiguity_decisions` без `UNCLASSIFIED_TABLE` | NOT_RUN | — |

Шаг 4, что проверить:
1. «Очередь проверки» → вид «нерешённая идентификация»: «Всего» равно числу открытых, «Дальше/Назад» листают.
2. Раскрыть упоминание: цитата, редакция, кандидаты с реквизитами; у кандидата с чужим ИНН в тексте — «Нельзя: …» и кнопка неактивна.
3. Выбрать кандидата без причины — кнопка «Записать» неактивна; с причиной — «Решение #N записано», в истории автор и время.
4. Во второй вкладке открыть то же упоминание до решения, записать другое решение — сообщение «Неоднозначность изменилась…», записи нет.
5. «Админка» → очередь слияний → «Предпросмотр»; в другой вкладке добавить доказательство или решение по сущности пары
   (например, решение по упоминанию из п.3, если кандидат — участник пары); «Слить» → «Предпросмотр устарел…», слияния нет.
6. Досье компании-цели после слияния: прежнее решение в «ранее проверено» с автором и временем, пометка «нужен пересмотр»;
   утверждение цели не помечено проверенным. Старый снимок открывается, hash прежний.

Что передать: хвосты логов шагов 1–3 с exit code; по шагу 4 — пункты PASS/FAIL (скриншоты без токенов и cookies); вывод compare шага 5.
