# Пользовательский прогон этапа 19 — ограниченный пилот
**Это форма, а не выполненный прогон.** Статус: **PILOT_NOT_RUN** — нет утверждённого манифеста, названных источников и оснований.
Агент пилот не запускает, источники не подбирает и мониторинг в фоне не ведёт. PowerShell, корень `TG_Info`.

## A. Без базы
| Шаг | Команда (cwd) | Изменяет данные? | Ожидается | Фактически | Exit/log |
|---|---|---|---|---|---|
| A1 | `cd backend; npx vitest run src/release/pilotManifest.test.ts` | нет | 7 passed | NOT_RUN | — |
| A2 | Скопировать `docs/development/pilot/pilot-manifest.template.json` в свой каталог, заполнить (владелец, цель, окно, источники с основаниями, проверяющие), `status: approved`, `decidedAt` | файл вне репозитория | в файле нет паролей, токенов и адресов с учётными данными | NOT_RUN | — |

## B. Цель и копия (до любого пилотного шага)
| Шаг | Команда | Изменяет данные? | Цель/guard | Ожидается | Фактически |
|---|---|---|---|---|---|
| B1 | Прогоны `evidence/10…18/USER_RUN.md` на итоговом коде (минимум: unit обоих пакетов, `npm run test:integration`) | тестовая схема | preflight | все passed | NOT_RUN |
| B2 | Копия и учения восстановления `pilot/OPERATIONS.md` §3 на целевой базе пилота | файл дампа; restore-цель | отдельная размеченная restore-цель | `release:manifest --compare` MATCH | NOT_RUN |
| B3 | `$env:DATABASE_URL` оболочки не задан; целевая база — в `backend/.env` (владелец проверил); `cd backend; npm run pilot:check -- --manifest <файл>` | нет (чтение) | имя базы = `target.database` | READY_TO_START, перечень сбора и ИИ; иначе BLOCKED с причинами — стоп | NOT_RUN |

## C. Пилот (строго по манифесту; каждый шаг — отдельное решение)
| Шаг | Действие | Изменяет данные? | Guard | Ожидается | Фактически |
|---|---|---|---|---|---|
| C1 | Сбор по источнику (`OPERATIONS.md` §1 п.2), не больше лимита | публикации, курсор | допуск на сбор; фон выключен | состояние источника и покрытие; полнота текста по редакциям | NOT_RUN |
| C2 | ИИ по источникам из `allowedSteps.ai` (§1 п.3) | запуски, наборы кандидатов (канон не меняется) | ИИ-допуск перед каждым вызовом | ошибки и неполные запуски видны в карточке | NOT_RUN |
| C3 | Проверка человеком и публикация по одному набору (§1 п.4–5) | канон | токен предпросмотра; автопубликация выключена | проверяющий видит цитату, редакцию, scope, неоднозначности | NOT_RUN |
| C4 | Обращения по субъектам манифеста → досье → снимок → выгрузки | обращения, снимки | — | «Кратко для переговоров» с ограничениями данных; основания открываются | NOT_RUN |
| C5 | Отзыв допуска одного источника в середине (контрольный) | статус источника | — | новые вызовы и сбор по нему прекращаются; старые материалы остаются | NOT_RUN |

## D. Фактические результаты (заполняет пользователь)
| Показатель | Значение | Как получено |
|---|---|---|
| Материалов разрешено по манифесту / загружено / полный текст / анонс / без текста | NOT_RUN | состояние источников, `--stats`, `/api/admin/sources/:id/health` |
| Разрешено к ИИ / запусков completed / partial / failed / cancelled | NOT_RUN | экран «Запуски», фильтр по источнику |
| Кандидатов публикуемых / на проверку / без цитаты | NOT_RUN | карточки запусков |
| Утверждений принято / отклонено / спорно / не установлено по ручной разметке | NOT_RUN | очередь проверки, решения |
| Пропуски, найденные ручной разметкой (с примерами) | NOT_RUN | сверка человеком с источником |
| Ложная идентичность / чужой договор / неверное отрицание | NOT_RUN | проверка досье |
| Обращений / досье / снимков | NOT_RUN | — |
| Фактическое время шагов (измерено) | NOT_RUN | часы оператора или логи команд |
| Сработавшие условия остановки (`OPERATIONS.md` §2) | NOT_RUN | — |

## E. Решение
Заполнить `docs/development/pilot/PILOT_DECISION.md`: GO_LIMITED / NO_GO / PENDING для этой области, причины, открытые риски.
Обновить `sources/VALIDATED_SOURCES.md` (перечень) и `quality/QUALITY_DECISION.md` (запись конфигурации), если они подтверждены.
Передать агенту: `pilot:check` вывод, логи шагов, таблицу D, решение. Агент анализирует и формирует корректировки, но не запускает пилот.
