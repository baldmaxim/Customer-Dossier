# Пользовательский прогон этапа 10
**Это форма, а не выполненный прогон.** Основные gates этапа 10 (T10-01…T10-09) подтверждены прогоном закрытия 09
(`evidence/09/USER_RUN_CLOSURE.md`, код `10d83d9` + `c289cb1`). Здесь — только то, что изменилось после него.

Команды — PowerShell, из корня `TG_Info`. Рабочая БД, `.env`, источники и модель не участвуют.

| Шаг | Команда (cwd) | Изменяет данные? | Цель/guard | Ожидается | Фактически | Exit/log |
|---|---|---|---|---|---|---|
| 1 | `cd backend; npx vitest run --maxWorkers=2` | нет | мёртвый адрес БД | все файлы passed, 0 skipped (≥ 35 / 499) | NOT_RUN | — |
| 2 (по желанию) | `$env:TEST_DATABASE_URL='postgresql://tg_test:tg_test@127.0.0.1:55433/tg_info_test'; npm run release:bench -- --runs 5` | да: снимки в `tg_info_test` | общий preflight (адрес, роль, маркер) | «отчёт действителен», ошибок timeout нет | NOT_RUN | — |

Что передать: хвост unit-лога с числом файлов/тестов и exit; вывод bench при шаге 2.
