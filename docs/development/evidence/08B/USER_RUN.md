# Прогон пользователя — этап 08B

## Прогон 1 — 2026-09-16, `dossier-stages @ 15c48ac`

| Шаг | Результат |
|---|---|
| A1–A3 typecheck, unit | ок — 29 файлов / 421 тест |
| A3 frontend build | ок |
| A4 тестовая база | ок |
| A5 `npm run test:integration` | ок — 17 файлов / 182 (в т.ч. `snapshot/snapshot.int.test.ts`) |
| M1 seed + сервер | ок |
| M2 схема связей: глубина, выбор ребра, период, отрицание | ок |
| M3 S1 неизменен, S2 с другим hash, фильтр дат, 422 на срез знаний в прошлое | ок |
| M4 выгрузки HTML / MD / JSON | ок; печать в PDF из браузера — не проверялась |
| M5 отзыв допуска источника, вымарывание, 401 без сессии | ок |
| M6 откат `GRAPH_EXPORT_ENABLED=false` | ок; после прогона флаг снят, `/api/graph` → 200 (Альфа-Демо / Берег-Демо / Бета-Демо) |

`PRINT_PDF=NOT_RUN` — печать снимка в PDF средствами браузера не проверялась; не блокирует 09 по решению пользователя.

Этап 08B — PASS по core-gates.
