# Прогон пользователя — этап 09

## Прогон 1 — 2026-09-16, `dossier-stages @ bc4aa36` + три локальных фикса (внесены в репозиторий следующим коммитом)

| Шаг | Результат |
|---|---|
| N1 схема с нуля, `seed:test-release` | ок после фикса seed (повторная публикация правки — с текущей `item_publications.version`, а не 0). Компании Порт-Релиз, Бета-Релиз, Альфа-Релиз ×2 (разные ИНН), Гамма-Релиз, Дельта-Релиз; объект «Причал-Релиз»; сигналы succeeded. Фактически публикаций 13, редакций 14 (в инструкции было 11/12 — исправлено) |
| N2 `release:check --out before.json` | ок: `local-inventory@1`, связность без нарушений, источники `demo_release_*` approved и синтетические, флаги сбора и разбора выключены. Фикс: проверка `revision_without_item` обращалась к `r.item_id`, верно `r.source_item_id` |
| N3 pg_dump → pg_restore в `tg_info_test_restore` → `--compare` | ок: совпадает с before.json. После удаления строки из `merge_queue` — расхождение и exit 1. (`DELETE FROM review_decisions` на данных seed удаляет 0 строк: решений в seed нет — инструкция исправлена на `merge_queue`) |
| N4 сквозной сценарий | ок через API: поиск одноимённых, цепочка договоров без транзитивного ребра, объект, обращение, противоречие, решение «спорно», снимок, выгрузки MD/JSON/HTML, hash снимка после перезапуска API прежний. Печать в PDF в браузере — не проверялась |
| N5 отказы | ок: PostgreSQL остановлен → API отвечает 500 (ошибка, не «сведений нет»); запущен → снова работает; `pipeline:once --check` — модель недоступна; отзыв ИИ-допуска → `--reextract` ставит в очередь 0; выход → 401 |
| N6 `release:bench --runs 5` | вывод ниже |
| N7 `npm run test:integration` | ок — 18 файлов / 196 тестов (после локальных фиксов теста и seed: версия публикации, предикат `participates_in_project`, фильтр поиска по имени) |
| N8 уборка | ок: `tg_info_test_restore`, дамп и json удалены |

## N6 — замеры (TC-078)

```
[bench] win32 x64 10.0.26200 · AMD Ryzen 7 8700F 8-Core Processor · ядер 16 · память 31.6 ГБ · Node v24.13.0
[bench] объём: sources=14, source_items=13, document_revisions=14, assertions=19, evidence=35, companies=7, projects=1, events=0, dossier_cases=3, dossier_snapshots=1
[bench] поиск компании по части названия: медиана 1 мс (мин 1, макс 2, прогонов 5) — HTTP 400
[bench] карточка компании (сведения и сигналы): медиана 9 мс (мин 7, макс 9, прогонов 5) — HTTP 200/200
[bench] резюме компании для досье: медиана 17 мс (мин 16, макс 17, прогонов 5) — HTTP 200
[bench] схема связей, глубина 2, лимит 60: медиана 5 мс (мин 5, макс 6, прогонов 5) — HTTP 200, узлов 3, связей 2
[bench] досье объекта: медиана 12 мс (мин 12, макс 13, прогонов 5) — HTTP 200
[bench] досье обращения: медиана 20 мс (мин 19, макс 22, прогонов 5) — HTTP 200
[bench] создание снимка досье: медиана 31 мс (мин 30, макс 33, прогонов 5) — HTTP 201
[bench] открытие снимка: медиана 5 мс (мин 4, макс 6, прогонов 5) — HTTP 200
[bench] выгрузка HTML («Версия для печати»): медиана 4 мс (мин 4, макс 5, прогонов 5) — HTTP 200, 14669 байт
```

Время поиска **не измерено**: замер шёл по старому пути `/api/companies/search` (HTTP 400, это время отказа, а не поиска).
Путь исправлен в `bc4aa36`; повторный замер поиска — отдельно.

`PRINT_PDF=NOT_RUN`. Этап 09 — PASS по core-gates.
