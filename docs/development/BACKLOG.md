# BACKLOG — дефекты и риски

## Статус после этапа 01 (2026-09-14)

| ID | Статус | Примечание |
|---|---|---|
| B-01 | закрыто | флаги заданий, по умолчанию выключены; smoke с одобренным источником |
| B-02 | закрыто | loopback, сессия, CSRF, Host/Origin |
| B-03 | закрыто | dry-run через каталог, destructive под флагом |
| B-04 | закрыто | unit-setup присваивает env, TZ=UTC, dotenv отключён; интеграционный guard |
| B-05 | закрыто | строгий разбор |
| B-06, B-07 | открыто, блокирует работу с рабочей БД | тестовая цель выделена (Docker `tg-info-test-db`); рабочая база не идентифицирована — решение пользователя |
| B-08 | частично | есть интеграционные тесты PostgreSQL; frontend-тестов и e2e по-прежнему нет |
| B-09 | закрыто | `npm ci` проходит; одна битая запись lock исправлена |
| B-10 | закрыто | `net/safeFetch.ts` |
| B-11 | закрыто | миграция 010, gate, источники заводятся на паузе |
| B-12 | предотвращено | удаление с документами выключено; модель доказательств — 02/03A |
| B-13 | открыто → 05A | |
| B-14 | открыто → 05B | |
| B-15 | закрыто (код) | вступит в силу с разблокировкой записи канона |
| B-16 | открыто → 02/07 | неизвестная дата публикации по-прежнему = сейчас |
| B-17 | открыто → 04 | |
| B-18 | закрыто | `SCHEMA_VERSION` убран из env |
| B-19 | закрыто | 009 применяется только с `--allow-destructive` |
| B-20 | смягчено → 07 | legacy-метка и формулировки; формула прежняя |
| B-21, B-22, B-23 | закрыто | SW без API/шрифтов, системный шрифт, описание |
| I-01…I-07 | закрыто | точечные правки CLAUDE.md/README/USAGE/CLI |
| I-08, I-09, I-10 | открыто | не входили в 01 |

Новые наблюдения этапа 01:

| ID | P | Наблюдение | Этап |
|---|---|---|---|
| B-24 | P1 | Рабочая база без миграции 010 не откроется в новом коде (выборки источников ссылаются на новые колонки). Нужны backup и согласованная миграция | решение пользователя |
| B-25 | P2 | R02: при неизвестном городе одноимённые объекты создаются отдельно; очередь слияний растёт, а слияние выключено | 04 |
| B-26 | P2 | Сессии в памяти процесса: рестарт API = повторный вход | осознанно, пересмотр при многопользовательском режиме |
| B-27 | P2 | `--bot-check` обращается к Telegram (`getMe`, `getWebhookInfo`) без допуска источника: это диагностика токена, сообщения не читаются | 05B — решить, нужен ли gate |
| B-28 | P2 | Отказ шедулера по допуску не пишется в `source_runs` — в админке виден через «Допуск», но не в «Последний запуск» | 05A/05B |

---

# Исходный список этапа 00

Дата: 2026-09-11. HEAD `6431a5b`. Все пункты установлены **статическим чтением кода**;
ни один не воспроизведён запуском (DB/UI/LLM — NOT_RUN, см. `evidence/00/COMMANDS.md`).
R01–R20 подробно — в [PATCH_COVERAGE.md](PATCH_COVERAGE.md); здесь только ссылки на них и новые находки.

Severity: **P0** — ложное досье, потеря решений или данных, небезопасный запуск; **P1** — нужно для ближайших этапов; **P2** — после стабилизации.
Gate: **блокирует** — нельзя переходить к зависимой работе без исправления; **ограничивает** — работа возможна, но с явной оговоркой.

## Запуск, окружение, тесты

| ID | P | Наблюдение | Место | Этап | Gate |
|---|---|---|---|---|---|
| B-01 | P0 | `npm run dev` сразу запускает сбор (тик 60 с, первый немедленно), LLM-воркер (30 с) и REFRESH метрик (10 мин, первый немедленно); флагов нет. Любой «просмотр портала» = сетевые запросы и запись в БД | `backend/src/index.ts:39-41,67-69,83-85`; `metrics/refresh.ts:54-56` | 01 | блокирует запуск портала |
| B-02 | P0 | API слушает все интерфейсы; авторизации нет; `POST /api/admin/merges/:id/merge`, `/reject`, `/metrics/refresh` принимают пустое тело (`decidedBy` по умолчанию `admin`) → выполнимы simple-POST с чужой страницы (CSRF) и из LAN | `index.ts:79`; `api/admin.routes.ts:141-176`; `app.ts:17-21` | 01 | блокирует |
| B-03 | P0 | `migrate --dry` выполняет `CREATE TABLE IF NOT EXISTS schema_migrations` до ветки dry | `db/migrate.ts:38` vs `:53` | 01 | блокирует проверку миграций |
| B-04 | P0 | Тестовый setup задаёт env через `??=`: уже экспортированный `DATABASE_URL`/`TZ` сохраняется; TZ по умолчанию `Asia/Almaty` | `__tests__/setup.ts:8,14` | 01 | блокирует DB-тесты |
| B-05 | P1 | `boolOf`: всё, кроме строки `false`, — true (`0`, `no`, опечатка → true). Для будущих флагов фоновых заданий опасно | `config/env.ts:26` | 01 | ограничивает (TC-010) |
| B-06 | P1 | Реальная конфигурация неизвестна: код читает `.env` из cwd (`dotenv/config`), `backend/.env` отсутствует; корневой `.env` содержит только `TG_BOT_TOKEN` и приложением не читается (env-check считает его «приманкой»). `DATABASE_URL` не задан нигде → target БД не определён | `config/env.ts:3`; `config/env-check.ts:13-29` | 01 | блокирует DB-тесты до выделения test-target |
| B-07 | P1 | На :5432 слушают `wslrelay` и `com.docker.backend` (PostgreSQL в WSL и/или Docker), Windows-службы нет. Какая из баз рабочая — неизвестно | E00-05 | 01 | блокирует DML до идентификации |
| B-08 | P1 | Интеграционных тестов с PostgreSQL нет; все 9 тест-файлов — чистая логика/фикстуры. Фронтенд без test/lint/e2e scripts. `worker.test.ts` проверяет число чанков, но не покрытие текста | `backend/src/**/*.test.ts`; `frontend/package.json:6-11`; `pipeline/worker.test.ts:24-28` | 01+ | ограничивает |
| B-09 | P2 | Установка зависимостей: lock-файлы есть, `node_modules` установлены; install-скрипты у 3 пакетов frontend (включая `sharp`) и 2 backend. `npm ci` удалит и переустановит `node_modules` | `*/package-lock.json` | 01 | ограничивает |

## Сеть и источники

| ID | P | Наблюдение | Место | Этап | Gate |
|---|---|---|---|---|---|
| B-10 | P0 | SSRF: `fetchText` без проверки схемы/хоста/IP, следует редиректам, читает ответ без лимита размера; ссылки из ленты и `<link href>` запрашиваются как есть; `POST /api/admin/sources/website` делает исходящие запросы сразу | `ingest/website.ts:46-61,64-87,118,244`; `api/admin.routes.ts:95-105` | 01 | блокирует (R10) |
| B-11 | P0 | Нет допуска источников (access/ai_processing); источник, добавленный через CLI или админку, сразу `active` — противоречит правилу «включать вручную после проверки» | `ingest/sources.ts:140,219`; `002_sources.sql` | 01 | блокирует (R11) |
| B-12 | P1 | `DELETE /api/admin/sources/:id?withDocuments=true` удаляет документы → каскад mentions/events/extractions/sightings; роли `project_participants` остаются с `evidence_document_id = NULL` (роль без доказательства) | `ingest/sources.ts:196`; `005_canonical.sql:64` | 01 защитить; 02/03A | ограничивает |
| B-13 | P1 | Найденный адрес RSS пишется в `cursor.feed_url`, но читается только `config.rss` → автопоиск ленты и лишние запросы на каждом проходе | `ingest/scheduler.ts:172-175`; `ingest/website.ts:220` | 05A | ограничивает |
| B-14 | P1 | Бот получает только `message`; правки (`edited_message`) не принимаются | `ingest/telegramBot.ts:217` | 05B | ограничивает |

## Конвейер и данные

| ID | P | Наблюдение | Место | Этап | Gate |
|---|---|---|---|---|---|
| B-15 | P1 | `--retry` после сбоя LLM не может записать успешный ответ: строки `extractions` с ошибкой остаются, новая вставка упирается в `ON CONFLICT DO NOTHING`; если `ok`-строк нет, `processDocument` возвращает failed **без обновления статуса** — документ висит в `extracting`, затем крутится queued↔retry. Успешный payload теряется; при частичных `ok`-строках канон привязывается к старому payload | `pipeline/worker.ts:154,232-243`; `pipeline/cli-commands.ts:155-162` | 03B (01 — адресный стоп, R06) | ограничивает |
| B-16 | P1 | Неизвестная дата публикации превращается в «сейчас»: `published_at ?? new Date()` в worker и ручной вставке; `mentions.published_at NOT NULL` → старый текст попадает в окно 90 дней | `pipeline/worker.ts:212`; `api/manual.routes.ts:51`; `005_canonical.sql:91` | 02/07 | ограничивает |
| B-17 | P1 | `enqueueMerge ... ON CONFLICT DO NOTHING`: пара в статусе pending не становится rejected при обнаружении конфликта ИНН | `resolve/company.ts:227-234` | 04 | ограничивает |
| B-18 | P2 | `SCHEMA_VERSION` объявлен в env (по умолчанию `extract@1`) и в test setup, но в `extractions` пишется константа `extract@2` из schema.ts — вводящая в заблуждение настройка | `config/env.ts:44`; `llm/schema.ts:9`; `pipeline/worker.ts:10,161` | 03B | — |
| B-19 | P2 | Миграция 009: `UPDATE events SET amount_rub = NULL`, пауза KZ-сайтов, `CREATE UNIQUE INDEX` без `IF NOT EXISTS`. Раннер применит её к любой базе без 009 без предупреждения | `009_switch_to_russia.sql:24,34,39-40` | 01 предупреждение | ограничивает |

## Интерфейс и PWA

| ID | P | Наблюдение | Место | Этап | Gate |
|---|---|---|---|---|---|
| B-20 | P0 | Светофор выдаёт «Без замечаний» и «Заметных проблем в открытых источниках нет» — отсутствие сведений читается как надёжность; индекс 40/30/30 не калиброван | `frontend/src/components/RiskBadge.tsx:20-23`; `pages/CompanyPage.tsx:50-51`; `007_metrics.sql:95-112` | 01 пометка legacy; 07 | ограничивает (R08/R09) |
| B-21 | P1 | Service worker: правило `urlPattern: /^\/api\//` NetworkFirst с хранением 24 ч. Workbox сверяет RegExp с полным URL, поэтому правило, вероятно, не срабатывает (статический вывод, в браузере NOT_RUN) — но замысел кэшировать API досье противоречит пакету. В `frontend/dist/sw.js` (сборка 09.09) правило уже есть | `frontend/vite.config.ts:26-36`; `frontend/dist/sw.js` | 01 | ограничивает (TC-009) |
| B-22 | P2 | Внешний шрифт Inter с Google Fonts обязателен в `index.html` и кэшируется SW на год | `frontend/index.html:20-25`; `vite.config.ts:37-55` | 01 | — |
| B-23 | P2 | Остатки казахстанской версии в описании приложения | `frontend/index.html:7`; `frontend/public/manifest.json:4`; `__tests__/setup.ts:14` | 01 | — |

## Противоречия инструкций (править в 01 точечно)

| ID | P | Противоречие | Где |
|---|---|---|---|
| I-01 | P0 | «После смены промпта переразбирать всё (`--reextract` без `--source`)» vs запрет массового reextract до нового apply | `CLAUDE.md` (раздел «Вклад документа…»), `README.md:148`, `docs/USAGE.md:148-157`, `pipeline/cli-quality.ts:185-190`, `pipeline/apply.ts:61-64` |
| I-02 | P0 | «Вклад документа в канон — ровно его последнее извлечение; apply сначала удаляет прежнее» — закрепляет R07 как правило | `CLAUDE.md`; `pipeline/apply.ts:45-65` |
| I-03 | P1 | «`--probe` запускать первым на новой машине» (живой запрос к t.me) vs запрет живого probe без допуска источника | `CLAUDE.md` (раздел «Команды»), `README.md:77-85`, `ingest/cli.ts:16-18`, `docs/USAGE.md:43,195` |
| I-04 | P1 | «После любой правки нормализатора — `--renormalize`» vs запрет автоматического/массового renormalize рабочей базы | `CLAUDE.md` (resolve/normalize.ts) |
| I-05 | P1 | «Светофор имеет состояние grey…» подаёт светофор как основу оценки; пакет требует объяснимые сигналы без рейтинга надёжности | `CLAUDE.md`, `README.md:191-195`, `docs/USAGE.md:83-90` |
| I-06 | P1 | Корневой `Odintsov/CLAUDE.md` (PWA): «`NetworkFirst` для `/api/`» vs «не кэшировать чувствительные API в PWA». Корневой файл вне репозитория — переопределять в `TG_Info/CLAUDE.md`, корневой не трогать | `../CLAUDE.md` раздел PWA; `frontend/vite.config.ts:27-36` |
| I-07 | P1 | `README.md:111` «`npm run dev` — API + ингест + пайплайн + бот» как штатный запуск vs безопасный локальный профиль по умолчанию | `README.md:108-113` |
| I-08 | P2 | «уверенность только растёт» (`GREATEST`) — одно сильное старое упоминание нельзя ослабить опровержением | `pipeline/apply.ts:195-197` |
| I-09 | P2 | «Удаление источника по умолчанию запрещено, если есть документы» — но `withDocuments=true` оставляет роли без evidence (B-12) | `CLAUDE.md`; `ingest/sources.ts:173-205` |
| I-10 | P2 | Корневой `Odintsov/CLAUDE.md`: «Без приписки Co-Authored-By» vs системная настройка атрибуции коммитов. К этапам не относится (коммиты запрещены), зафиксировать для пользователя | `../CLAUDE.md` раздел Git |
