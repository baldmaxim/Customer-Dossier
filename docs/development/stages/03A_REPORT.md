# Отчёт этапа 03A — Утверждения, доказательства и решения аналитика

Дата/время: 2026-09-14 (+02:00). Исполнитель: Claude Code (Opus 5), единственный writer.
Статус: **IN_PROGRESS — ждёт прогона пользователя** (интеграционные тесты, backfill CLI, панель).
В среде агента: typecheck и сборки backend/frontend, unit 21 файл / 311 тестов — PASS.

## Исходная база

- Ветка `dossier-stages`, этап 02 закрыт коммитом `bc8dc03` (PASS по прогону пользователя).
- Stage prompt: `prompts/Customer_Dossier_Prompts/stages/STAGE_03A_ASSERTIONS_EVIDENCE.md`; зависимость — `02_REPORT.md`.

## Что изменилось для пользователя

- В админке появилась «Проверка утверждений»: что утверждают источники (роль на объекте, событие, упоминание),
  поддерживающие и опровергающие цитаты рядом, с контекстом и ссылкой на версию публикации.
- Решение аналитика (подтвердить / спорно / отклонить / снять оценку, с причиной и тем, что именно проверено)
  хранится отдельно от вывода модели, не пропадает при переразборе и не затирается второй вкладкой.
- Если доказательство отозвано или появилось опровержение, прежнее решение остаётся в истории, а утверждение
  попадает в «Нужен пересмотр».
- Старый канон переносится командой `npm run backfill:assertions` (dry-run по умолчанию): только с реально
  найденной цитатой; ручные статусы событий сохраняются с пометкой «обоснование не сохранилось».

## Изменения

| Файл/миграция | Суть | Совместимость/риск |
|---|---|---|
| `docs/migrations/012_assertions_evidence_reviews.sql` (новый) | `assertions` (FK сторон, CHECK формы предиката и разрешённости, неизменяемое содержание), `evidence` (проверка цитаты триггером, отзыв статусом), `review_decisions` (append-only) | только расширение |
| `backend/src/assertions/span.ts` (новый) | offsets в code points, все вхождения цитаты, однозначное нахождение, контекст | — |
| `backend/src/assertions/model.ts` (новый) | ключ содержания, хэш набора доказательств, вывод состояния | — |
| `backend/src/assertions/repository.ts` (новый) | upsert утверждения, добавление и отзыв доказательства, решение с версией и идемпотентностью | — |
| `backend/src/assertions/backfill.ts`, `cli.ts` (новые), `package.json` | перенос mentions / project_participants / events, отчёт, checkpoint, advisory lock; `backfill:assertions` | рабочий backfill не выполнялся |
| `backend/src/api/assertions.routes.ts` (новый), `app.ts` | `GET /api/assertions`, `GET /api/assertions/:id` (ETag), `POST /api/assertions/:id/reviews` (201/200 replay/409/422), `POST /api/evidence/:id/withdraw` | под входом оператора и CSRF; лимиты выборок |
| `frontend/src/components/AssertionReviewPanel.*`, `AssertionDetail.tsx`, `lib/describeAssertion.ts` (новые), `pages/AdminPage.tsx`, `api/types.ts`, `lib/labels.ts` | панель очереди и карточка утверждения с решением и отзывом | AdminPage 362 строки |
| тесты: `assertions/assertions.test.ts` (unit); `assertions/assertions.int.test.ts`, `assertions/backfill.int.test.ts` (интеграция); `__tests__/integration/seedAssertionsDemo.ts`, script `seed:test-assertions` | см. «Тесты» | интеграция — у пользователя |
| `docs/development/ADR-003-assertions-evidence-reviews.md` (новый), `CLAUDE.md`, `README.md`, `TESTING_LOCAL.md` (раздел E) | решения и инструкция | — |

Legacy mentions/events/project_participants, карточки и API чтения не менялись. Конвейер на новую запись не
переведён (03B), блокировки `pipeline/guard.ts` сохранены.

## Тесты

| Gate/сценарий | Команда | Target | Exit | Статус | Лог/наблюдение |
|---|---|---|---|---|---|
| Typecheck backend/frontend | `tsc --noEmit` | — | 0/0 | PASS | среда агента |
| Сборка backend/frontend | `npm run build` | — | 0/0 | PASS | среда агента |
| Unit | `npm test` | мёртвый URL | 0 | PASS: 21 / 311 (было 297) | среда агента |
| TC-024 кириллица, эмодзи, NBSP, повтор цитаты (логика) | `assertions.test.ts` | unit | 0 | PASS | — |
| TC-019 два доказательства, отзыв одного, решение и второе на месте | `assertions.int.test.ts` | tg_info_test | — | ждёт прогона | TESTING_LOCAL A5 |
| TC-020 новый смысл без наследования решения; UPDATE содержания отвергнут | то же | tg_info_test | — | ждёт прогона | |
| TC-021 опровержение рядом с поддержкой | то же | tg_info_test | — | ждёт прогона | |
| TC-022 идемпотентный повтор, 422 на другой payload, 409 второй вкладки, 401/403 | то же | tg_info_test | — | ждёт прогона | |
| TC-023 FK на компанию/редакцию, CHECK формы и разрешённости | то же | tg_info_test | — | ждёт прогона | |
| TC-024 цитата не совпала — отказ базы; эмодзи — offsets JS приняты базой | то же | tg_info_test | — | ждёт прогона | |
| История неизменяема (UPDATE решения, DELETE/UPDATE доказательства) | то же | tg_info_test | — | ждёт прогона | |
| TC-025 legacy: только найденные цитаты, ручные статусы = контрольная выборка, автостатус не подтверждение, повтор | `backfill.int.test.ts` + TESTING_LOCAL E1 | tg_info_test | — | ждёт прогона | |
| Панель в админке, две вкладки, отзыв, 390 px | TESTING_LOCAL E2 | tg_info_test | — | ждёт прогона | |
| Регрессия этапов 01–02 (7 файлов / 61) | `npm run test:integration` | tg_info_test | — | ждёт прогона | ожидается 9 файлов / 82 |

## Данные, безопасность и откат

- Рабочая БД не подключалась; миграция 012 и backfill к ней не применялись. Docker в среде агента не запускался.
- Флаги не менялись. Откат: не использовать панель и API утверждений (legacy-чтение не зависит от них); таблицы 012
  и данные не удалять — они append-only. Старый apply не включать.

## Непроверенное, дефекты, решения

- Все сценарии с базой ждут прогона пользователя.
- Утверждения из конвейера не создаются до 03B; модальность legacy — `unknown`.
- Для роли на объекте доказательство берётся из упоминания той же компании с той же ролью в документе-основании;
  если такого упоминания нет — роль не переносится (счётчик `quoteNotFound`).
- Набор предикатов минимальный; договорные и корпоративные связи — этап 06.
- «Пересмотр» срабатывает на любое изменение набора активных доказательств, в том числе на новое поддерживающее —
  консервативно; уточнение правил — по опыту использования.

## Завершение

Следующий промт после подтверждения прогона: **`prompts/Customer_Dossier_Prompts/stages/STAGE_03B_SAFE_REPROCESSING.md`**.
